# Plant_smORFeus — Python dependencies
#
# Install order matters:
#   1. Install PyTorch with CUDA first (see https://pytorch.org/get-started/locally/)
#   2. Install mamba_ssm from source (requires CUDA-capable GPU)
#   3. pip install -r requirements.txt
#
# mamba_ssm MUST be installed from source — it is NOT available on PyPI:
#   pip install mamba-ssm causal-conv1d
#   (see https://github.com/state-spaces/mamba for full instructions)

# ── Deep learning framework ────────────────────────────────────────────────────
# Install torch separately with the correct CUDA version for your system:
#   pip install torch==2.2.0+cu121 --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# ── Lightning training framework ───────────────────────────────────────────────
lightning>=2.0.0
pytorch-lightning>=2.0.0

# ── Hugging Face ecosystem (for config base classes) ──────────────────────────
transformers>=4.35.0

# ── Data pipeline ─────────────────────────────────────────────────────────────
h5py>=3.9.0
numpy>=1.24.0
tqdm>=4.65.0

# ── Genomics I/O ──────────────────────────────────────────────────────────────
biopython>=1.81
gffutils>=0.12.0

# ── Metrics and evaluation ────────────────────────────────────────────────────
scikit-learn>=1.3.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Config serialisation ──────────────────────────────────────────────────────
pyyaml>=6.0

# ── mamba_ssm (CUDA GPU required — install from source, not from this file) ───
# mamba-ssm       # DO NOT install via pip install -r; install from source:
#                 # https://github.com/state-spaces/mamba
# causal-conv1d   # required dependency of mamba-ssm
